{
  "name": "@causalmap/basic-example",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@causalmap/core": "0.1.0",
    "@causalmap/renderer": "0.1.0"
  }
}